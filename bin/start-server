#!/bin/bash
# author: christian calloway callowaylc@gmail.com
# description: kicks off punicorn master processs

# start process, redirect and attach nohup policy
nohup php-cgi ./init config=./web-root/config/punicorn.php 2>&1 &

# copy master mid to process list
echo $! > ./tmp/pids/servers.pid
